# CR-119 local validation

Safety checkpoint: `7cc2f1da14b16b136746c575355c73067708860f`. This also checkpoints the completed 0.20.1 feedback delivery. That versioned runtime/ZIP and normal `Builds/Latest` remain preserved. No course, animal, title audio, ghost, records, or broader radio regressions were run for the launcher.

- Native Win32/GDI+/XInput prototype launched the actual 0.20.1 game with the correct working directory. Parent remained alive while the child ran and exited after its normal exit. Keyboard/controller action-selection model passed. Physical controller input and Deck were unavailable, and are not claimed.
- Shared native core: 15 bounded fixtures pass (`core-fixtures.json`): signed initial inventory, old/new activation, save backups, rollback, offline access, unsigned/altered signature rejection, corrupt ZIP, cancellation, running-child exclusion, traversal rejection, insufficient storage, interrupted activation, soundtrack add/change/withdraw and preservation, independent pointers, interrupted soundtrack recovery.
- First fixture run exposed a Windows atomic-file replacement conflict. Bounded retries for sharing/access conflicts preserve the old pointer and report the Win32 error if retries fail. A second fixture exposed slash-format mismatch in running-process identification; filesystem identity comparison fixed it. Affected fixture runs pass. No safety check was bypassed.
- Actual 225,757,700-byte game-only signed ZIP installed over build21001 to build21002 through the native core. Updated game startup and rollback startup both confirmed the nonce handshake, parent lifetime, normal exit0, isolated muted settings and byte-identical playlist. Same managed/personal paths remained in use. Current user saves were never used for fixtures.
- Updated runtime discovered all four bundled station names plus the personal fixture station. Radio was deliberately Off and master0; the logged selected-station Count is therefore0. Full soundtrack inventory verification proves all187 playable songs and two original files are present. No listening/general music suite was performed.
- A fixture initially compared serialized Unity floats with exact Python decimal equality; it was corrected to a 1e-6 tolerance and the successful startup evidence reused. No duplicate game launch was needed. A resume-path assertion was similarly normalized before rollback. These were fixture assertions, not lost player preferences.
- Final Unity build: Succeeded,0 errors,4 warnings. Native bootstrap uses static CRT; no separately installed desktop runtime. Existing signed Windows binary trust/SmartScreen reputation is not asserted. Public-key release signatures are separate from executable code signing.
- Complete starter ZIP: 418 files,1,017,500,040 bytes. All extracted files match staged hashes. SHA256 `74a2c78a0e00158d402ed8aedfb2ac377997ec03a78042d9fd6333fefa82048d`. Approved cover is included separately. Runtime contains220 files; designated soundtrack189. Publisher signing private key, project source and fixture saves are excluded.
- Publisher dry run:194 assets,2,050,515,998 bytes total; every asset under2GiB. Independent soundtrack dry run retains the game pointer and reuses all189 unchanged song URLs, producing only two metadata assets. No upload or authenticated permission check ran.

Remaining acceptance: physical Deck/Steam Input/controller behavior, existing non-Steam AppID/prefix migration, and Dan's launcher UX review. Timeout/early-exit recovery UI and migration helper are implemented but not physically exercised on Deck. The Windows tests used isolated fixture saves, not Dan's live progress. No claim of physical or human approval.

Evidence: `prototype/`, `core-fixtures.json`, `integration/results.json`, updated/rollback startup logs and screenshots, `package-verification.json`, and `publisher-dry-run.json`.

Publication follow-up: Dan approved public release build-21002. All 194 remote hashes/sizes match the staged inventory. Public catalog/signature checks and actual 225757700-byte hosted update 21001 to 21002 passed; muted startup returned nonce-ready, parent lifetime held, child/launcher exited 0, playlist bytes remained unchanged. See publication.json and hosted/result.json. No additional gameplay/audio tests. Earlier prepublication statements above describe the local-delivery checkpoint.

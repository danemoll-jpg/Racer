# Woodstock Rush 0.14.0-review2 correction delivery

CR-082–089 are implemented for review, with CR-087 explicitly only partly resolved. This is not a claim of human acceptance or a clean result for every ramp approach. Internal Racer executable, project and save paths are preserved. No external upload. CR-081 ghosts, collectibles, larger neighborhood and photo-based home work remain approved and queued after this correction pass. Split screen/networking remain out of scope.

Safety checkpoint: `b1d076d948e2a8ce57bdfaeebd698b87e77bd5b6`. Staging and committing initially hit sandbox permissions; elevated retries succeeded. No saved changes, locks, hooks, signing or history were discarded. The completion commit is stamped into the delivered `VERSION.txt`; the source manifest covers 2,883 Assets/Packages/ProjectSettings files. Final compiled assembly SHA256: `4C3CEB98C70F971631096357E788866D770CD9C2A75AF08C4166F3F3F5E83012`.

## Every reported issue

| Request | Implemented correction and evidence | Remaining acceptance |
| --- | --- | --- |
| CR-082 people | Removed helmets; enlarged readable eyes/brows/nose/mouth, hair and ears; distinct skin/shirt/trousers/shoes; lowered car seating to clear roof, aligned controls. No whole-occupant paint override was found: the prior orange shirt/helmet palette obscured faces. All seven paint choices preserve occupant materials and physics. Actual compiled garage/front/side/chase/face views for all four profiles: [gallery](PEOPLE.md), art 116/116. | Dan's visual judgement of faces, posture and style. Shared materials are used; concurrent hidden tests are not an FPS benchmark. |
| CR-083 household exclusivity | One four-state shuffle bag across BOTH properties: coffee, football, smoking, nobody. Clear pooled figures/smoke before activation; preserve seed/state through laps, pause and local recovery. New sessions vary without swapping live groups. Tests cover all states, saved/reloaded preference, all courses, restarts and race/roam. | Human observation of variation over ordinary sessions. |
| CR-084 menu radio | Course selection reloads the scene; the old scene-owned radio restarted. Persist one audio source and reattach it to the new flow, keeping channel, clip and playback position. Existing driving-only controls remain. 41/41 tests across all courses, repeated selection, synthetic keyboard/gamepad navigation, menu return, saved preferences, explicit next song and natural completion. | Physical controller and listening tests. Synthetic input is not a physical device test. |
| CR-085 checkpoint edges | Sweep the actual oriented vehicle box through the crossing plane, accepting legitimate opening overlap plus 0.20 m (7.9 in). Align rendered posts' inner faces with detection. All eligible sizes, both edges, ground/air and start/finish tested; outside negatives, backward/duplicate prevention, first-lap arming and exactly +5 once retained. Authorized shortcut bypasses recomputed. | Dan's original visually-inside edge case and fast jumping passes. |
| CR-086 imperial units | HUD/traps/help/activities/targets/results/records and physical signs use mph, miles or feet. Canonical physics/save data and medal comparison precision unchanged. Target rounding avoids advertising a lower threshold. Legacy preference migration does not scale saved numbers. | Human scan of menus/HUD and records. |
| CR-087 Trickum | Baseline tested FIRST; reproduced reverse contact loss fixed and reverse side shoulders widened. Exact cause, before/after and failures below. Standing baseline/changed/final ramp requirement added to TODO and ramp policy. | Forward-layout reported unexplained slowdown remains unconfirmed; forward instability and one outer-edge motorcycle roll remain open. |
| CR-088A first reverse shortcut | Direction-facing physical advance and mouth signs, supported approach and cleared sightline. Final images and actual multi-lap AI/player traversal retained. | Dan's approach readability. |
| CR-088B floating tree remnants | Remove 39 coherent obstructing trees, rebuild complete trunks/crowns of 11,736 remaining trees in 90 reverse-only spatial mesh batches. No LOD/billboard layer on this tree root. Generator retains whole-tree exclusions. | Human inspection for remaining distant fragments; no claim every camera position was inspected. |
| CR-088C third forward shortcut exit | Pine Ridge is not offered as a reverse entrance; reverse-only straight/main and EXIT ONLY cues. Forward exit remains intact. | Human reverse approach view. |
| CR-088D reverse grid/finish | Named finish was gate array element 17 while gate zero was CP1. Make the actual named finish gate zero; align grid 32 m before it and use that crossing for timing. First-lap arming/duplicates checked, three laps completed. | Human grid and finish appearance. |
| CR-088E redundant ramp gates | Remove launch crossing at route station 1524.109; move the next crossing from 1594 to post-landing station 1690. Next gate at 1844 is 154 m away; no mid-launch required gate. Gate order, bypass sets, AI, HUD, recovery/wrong-way origin and timing categories coordinated. | Human approach/landing clarity. |
| CR-089 Forest Reverse departure | Grid and finish moved onto supported main lead-in before first fork; main-bend and optional-shortcut physical signs, smaller uncluttered boards. Main route remains the continuation. Both route choices tested with motorcycle/ATV from actual grid under normal driving, no position forcing. 12/12 and initial/3/6/9/12/15-second images. | Dan's first driving view without diagnostic overlays. |

## Ramp cause and actual evidence

[Baseline report](BASELINE.md) retains original installed 0.13.0-review7 race matrices (120 traversals) and the instrumentation-only baseline free-roam matrices (240). The original compiled probe could not reverse free-roam direction, so only diagnostic switches/telemetry were added before the second baseline build; gameplay/geometry hashes were checked. No correction preceded these tests. Forest-loaded ambient copies were inventoried initially and driven additionally in final validation; do not treat that as a pre-edit physical Forest matrix.

Actual ramp IDs are `Phase 4 - Connector Jump/Takeoff - 40m x 6m, 6.2m rise` (`TrickumForward.asset`) and `Reverse course authoring/Reverse supported roadworks transition` (`TrickumReverse.asset`), frame near (-626.16, 7.80, -109.99). Forest ambient forward copies are `TrickumLakeWoods.asset` and `TrickumForestLoopReverse.asset`. All three forward mesh files remain byte-identical (SHA256 `97C225A17AC42E5F525968244FE71DDBF9679DD661A8239BF02B57AF500E23D8`).

The reverse mesh name was missing from supporting-surface contact correction, and sweep CCD contacts were not subscribed. Reproduced motorcycle target 12 m/s, lateral -3, lost 11.859 to 6.665 m/s in one 0.02 s step with four suspension probes grounded, upright 0.994, brake zero and no water. Contact-only corrected build traversed that step 11.859 to 11.898 m/s. Opposite-direction reverse-layout free roam also reproduced a Classic dropping total velocity 34.989 to 3.333 m/s with brake/water zero and upright 0.979; contact repair removed this snag. Correction uses authored supporting faces, including CCD's inverted normal convention, scoped to the two Trickum meshes. Side/underside contact remains physical; there is no added propulsion, ignored contact or global collision disable.

Contact repair alone retained an ATV side-edge roll at 24 m/s, line 1.7 (minimum upright -0.941). The reverse shoulder was steep: 3.2 m rise across 2 m. Widening it to 6 m retained the 6 m top, 42 m run, 3.2 m rise and 24 m rollout; the same ATV case then stayed upright (minimum 0.959). New actual outer shoulder lines -10.3 and 7.3 were separately tested. The original forward ramp and handling were not changed.

Raw trace CSVs contain input, speed before/at/after each transition, total velocity, water, support, suspension/stability and contact information. Original traces have less contact detail than the later diagnostic build; no missing baseline fields are retroactively invented. [Final results](release2/RESULTS.md) and [retained cases](release2/ramp-retained-failures.csv) are the authoritative case counts. Speeds are canonical telemetry: 12/24/36/43 m/s = 26.8/53.7/80.5/96.2 mph. Five center/off-center/bevel lines are used for each eligible profile; additional new shoulder-edge cases run at 24 m/s.

Final compiled review2: **664 traversals, 143 explicitly non-clean cases, zero failed local recoveries**. The 240 reverse-layout core/high-speed cases (race and both free-roam directions) stayed above upright 0.65 and completed. Of 24 additional reverse outer-edge cases, the motorcycle opposite-direction free-roam approach at -10.3 rolled (minimum upright -0.164), completed and recovered. This remains a failure of clean edge traversal. The other 142 non-clean cases are on the unchanged forward ramp, including the identical ambient copies loaded by Forest variants. Core forward race/free-roam outcomes match baseline; high-speed cases also include tumbles/unstable landings. A `completed` outcome alone does not mean a clean traversal.

Forward-layout drops examined include yaw changing forward-projected speed and landing removing downward velocity. These do not establish the same unexplained contact snag Dan reported. That report is **not closed** by the reverse fix, fixture completion or successful recoveries. The new outer-edge roll also remains open. Candidate `contact1`, `shoulder1`, `systems1` and `final` (review1) evidence is retained; only `release2` is the final code/scene build.

`release2-speed-events.csv` also retains ordinary race/departure speed changes, which must not be mistaken for ramp failures. In the reverse ramp matrices, remaining abrupt losses above 1 m/s occur on ground landing contacts: high-speed Tourer/ATV on `Ground_80_240` around z=-132 to -135, and opposite Tourer on `Ground_80_320` around z=-6.2. Normals are approximately up, brake/water zero, with landing impulses and removal of vertical velocity. These are recorded rather than suppressed; no repeated baseline incline snag was observed. Outer-edge motorcycle events include yaw/roll and remain separately flagged.

## Automated regressions and limits

- Systems 474/474: actual profile selection, units and unscaled legacy saves, shared household states, every course's gate edges and start/finish. The earlier `systems1` 460-check fixture selected profiles in the wrong menu state; its profile coverage is superseded, not used as proof.
- All four three-lap races: 6/6 checks each, all 16 participants finished 3/3 laps with zero missed-gate penalties, seed 82089. Ordinary motor/AI driving, accelerated test time; no lap teleporting. Local recoveries occurred and are retained in CSVs.
- Rules 96/96, local recovery 282/282, roam recovery 80/80, speed traps 144/144, smash 24/24, guidance 24/24, Forest scoring 12/12, records 29/29 and AI finish estimates 96/96.
- AI obstruction/recovery 35/36. Forward Street Tourer did not clear the fixture's 60 m progress threshold in 35 seconds (two recoveries). At its deadline it had traveled 26.55 m and was moving at 23.32 m/s; it was not indefinitely stuck in the observation window. All sustained-stall recovery tests passed. The failed deadline remains in `release2/ai-StreetLoopGreybox`.
- Earlier candidate Forward Street race seed 158927500 had a player DNF at the normal 90-second finish grace after a north-corner recovery (2/3 laps; moving again at 11.08 m/s when ended). Other three finished. Final seed success does not erase this retained failure in `systems1`.

All four timing categories are versioned: `street-v12-corrections`, `lake-v5-corrections`, `street-reverse-v3-corrections`, `forest-reverse-v3-corrections`. Historical save entries remain. Unchanged stunt course identities explicitly map to previous categories to retain personal bests; the revised reverse ramp uses a new activity category. Save/record comparisons remain canonical. Free roam, stunt scoring, traps, water, wildlife/audio, local recovery, radio packaging and top-ten/estimate systems remain present; no physical listening, hardware-controller or comprehensive performance benchmark is claimed.

## Package and Dan's review

Versioned Windows x64 runtime: `Builds/Racer-0.14.0-review2-Windows`; regular entry: unchanged `Play-Racer.cmd` or complete `Builds/Latest/Racer.exe`. `Tools/Package-Racer.ps1` stages only `BundleMusic`, preserves old complete runtimes/music/ZIP, then verifies every extracted ZIP, versioned-runtime and Latest file by SHA256. `Builds/PACKAGE-LATEST.json` records the final package paths/counts/hash. Packaging and Latest smoke results are recorded in [PACKAGE.md](PACKAGE.md). Tested executable/assembly and scene data are copied intact; packaging does not rebuild them.

Dan should prioritize reverse Trickum with the motorcycle/ATV, then the exact free-roam slowdown in both directions and all loaded course variants. Try centered and bevel approaches first; inspect the retained outer-edge motorcycle and forward instability cases. Then check all four occupant profiles in garage/race, all household states across new sessions, radio continuity with a physical controller while selecting courses, both checkpoint edges and outside misses, imperial labels, Reverse Street's first entrance/whole trees/exit cues/grid/landing gate, and Forest Reverse's main-route departure and optional shortcut. Human driving, visual acceptance, listening and physical-controller tests are pending and are separate from the automated evidence above.

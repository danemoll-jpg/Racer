# CR-112–118 / 0.20.0-review1

Course-review preview under Dan's reduced test scope, issued 2026-09-21. The earlier four-lap/all-vehicle/center-and-offset/alternate/AI-event matrix is superseded. Extensive mountain/AI testing has stopped; no additional track runs are authorized before layout review merely because an automated driver fails. Existing route and initial flight evidence is used for this preview. Both courses remain NOT ACCEPTED pending Dan's layout/fun review; AI and incomplete vehicle coverage remain unverified. Approval of the layout must not automatically trigger extensive testing: only targeted checks of new changes or reported issues, especially AI.

Safety checkpoint: `16ccff01e05c666d312c02df443dd4b22564f7a1`. Git permission failures were retried through supported elevation before edits. No changes discarded, lock files deleted, hooks/signing bypassed or history rewritten.

## Scope and evidence

Only the seven requested items and their observed direct dependencies were checked. All ordinary players use isolated temporary saves and Master mute. Turkey audio is unchanged. Ghosts were accepted by Dan on 2026-09-21 and were neither changed nor tested. Existing unrelated defects remain historical records, not new work in this pass.

- Household positions and natural visits: `final/households`. The first approach fixture wrapped its distance at the starting point and did not drive; those `first/households` views are superseded. The corrected run physically covered 65 metres on each ordinary approach, reached all three groups plus empty, and retained the independent two campers. Street captures visibly show two coffee drinkers outside Dan's fence, three football players inside his yard and two smokers on Kyle's street-facing approach. The later canopy cleanup retains these placements.
- Naming and championship data rules: `final/naming/checks.txt`. Five keyboard/edit/paste/save/cancel/isolation assertions pass. Seven small scoring/identity/replacement/tie/solo assertions pass. The first fixture selected four of the five letters in "Night", leaving an extra `t`; its failure was corrected in the fixture, not hidden by changing the expected name. Standard uGUI event processing and a virtual keyboard were used; physical keyboard/controller/Deck hardware is not claimed.
- Initial inner highway joins and frontage: `first/roads`. Both normal drives have zero unsupported intervals; affected through-traffic was observed. The local Kyle driveway edge drive passed. Before/after views and mesh-owner audits are retained. Dan's two legitimate drives and House 3's drive were retained.
- Turkey-only final visits: `final2/turkeys`. A naturally selected, visibly grounded three-turkey group and an absent visit pass. The two-of-six saved visit bag remains occasional. Old habitats were behind the current property's roadside crest; a retained visible group also prevented the pending schedule from being consumed. Reset selection now commits before the first new world frame. No turkey sound was changed or tested. Dan's sighting remains pending.

## Mountain geometry and retained failures

Both courses remain NOT ACCEPTED, awaiting Dan's redesign/fun review. The main lines contain both a gully flight and the summit flight; gold entrances identify two extra optional routes per direction. New layout IDs are `mountain-forward-v2-main-flights` and `mountain-reverse-v2-main-flights`. Historic incompatible data is retained; ghost logic is unchanged. Actual route maps are `route-MountainLoop.svg/png` and `route-MountainLoopReverse.svg/png`.

The eight first-playable centered motorcycle/ATV flight cases passed using ordinary motor inputs from rest, without injected launch velocity. Gully flights: 41.92–43.86 m/s takeoff, 5.88–6.12 s airborne, 189.54–206.02 m. Summit flights: 40.86–42.84 m/s, 7.02–7.52 s, 204.13–227.72 m. All achieved stable wheel-supported landings. The initial bare outer region was then filled with a wooded ravine; the initial gully `clearance=999` means no underlying ground ray hit, not a real 999-metre measurement.

The first complete-lap attempt (`final/playlist`, candidate1) was stopped after a start-corner recovery and an almost-180-degree gully rejoin failure. The rounded corner and direction-aligned rejoin replace those connections. During authoring, replacing a guidance root also removed its child gate template; that failure was recovered from the intact reverse-scene template, and gates are now reparented before guidance replacement.

The next attempt (`final2/playlist`, candidate2) passed the rounded start, but the rider was braked to 17.72 m/s on the runway and landed in the ravine. Inspection proved an active retained `CR056 Forest Loop` component was applying unrelated station limits (340/610/900/1020/1310/1540) to the new mountain road. Mountain drivers now use only their own flight metadata; no vehicle forces or launch velocity were changed. Intentional-flight navigation uses the same existing normal motor inputs as other drivers.

## Outer highway dependency

`outer-endpoint-source-audit.json` identifies the original east continuation's last five-metre quad dropping 33.7 m: the terrain sampler returned zero beyond its terrain tiles. The corrected endpoint retains full four-lane width and extends supported pavement. The first outer drive (`final2/endpoints`) then encountered the old logical return boundary inside that new pavement. Its bounded return limit now follows the CR113 highway endpoint; other road boundaries retain their original limits. No physical road-end wall is introduced. Only these failed outer segments need repeating; the passed inner-seam/frontage drives are complete.

The third attempt (`final3/playlist`, candidate3) restored full runway acceleration and produced a 46.24 m/s, 6.40-second, 227.33-metre gully flight with a wheel-supported landing. It then overturned on the climbing connector after the rejoin at approximately 34 m/s. The run stopped at the first automatic recovery. Mountain AI now caps its ordinary connector target at 24 m/s while preserving full acceleration in the committed flight run-ups. The next candidate must verify this correction; the failed attempt is not a completed lap.

Corrected outer endpoint drives (`final3/endpoints`) both pass: minimum upright orientation 1.000, zero unsupported time, and through-traffic observed at each changed end. No further highway driving checks are pending.

Visual review of those outer captures then identified obsolete `Distant tunnel backdrop` panels across the road at local station 429. The active mesh-owner audit distinguishes these non-colliding panels from the legitimate tunnel sides/roof and new supported pavement. The panels were removed in all six scenes and in the regeneration recipe. A final visual check remains; the previous driving pass alone did not establish visual acceptance.

The revised final-player views and short drives (`final5/endpoints`) pass after backdrop removal. Both captures were inspected: four-lane pavement and markings continue beyond the retained open tunnel roofs/sides, with no backdrop across the lanes. Both drives retain minimum up 1.000 and zero unsupported time; through-traffic was present. This closes the endpoint visual dependency.

The fourth attempt (`final4/playlist`, candidate4) passed the gully and climbing connector, then reached the summit flight (39.78 m/s, 7.60 s, 217.72 m), but landed off the intended catch lane and needed a recovery on the return. Thus neither a valid full lap nor adequate summit landing clearance is claimed for that attempt. Actual road metadata shows a straight launch/catch bearing; the AI's 3D pursuit target could deviate during ascent. The next candidate follows the horizontal launch bearing with ordinary steering inputs across that segment. No velocity or position is injected to produce a flight.

The fifth attempt (`final5/playlist`, candidate5) exposed a regression in that navigation change: fixed launch-lane selection put all four racers in a line, defeating the existing passing choice. The chase capture shows all three rivals directly ahead; braking reduced the gully takeoff to 16.39 m/s and the rider landed on the ravine floor. The failed run stopped at recovery. The next candidate retains horizontal bearing but restores the driver's existing passing-lane choice, with obstacle-name/target-speed diagnostics in the bounded lap CSV.

The sixth attempt (`final6/playlist`, candidate6) restored the gully flight (44.51 m/s, 6.18 s, 211.85 m) and reached the summit catch slope (39.84 m/s, 6.72 s, 191.35 m). It failed at the old summit return hairpin/drop; the isolated player was stopped after that observed failure. The forward mountain scene now has a supported downhill return instead of that hairpin, and its Summit Traverse rejoins the new return. The other four scenes' accepted free-roam summit geometry is retained.

Local candidate6 checks passed forward Summit Traverse, reverse Downhill Ridge Cut, and one recovery in each direction. Forward Climbing Ridge Cut and reverse Summit Traverse failed. Their telemetry and driver source exposed premature branch-plan removal when the nearest main-road station jumped beyond the branch's exit while the racer was still on the branch. The corrected driver retains its branch plan until branch tracking exits. Retry only the two failed alternates and the forward Summit Traverse rejoin changed by the summit-return correction; the two passed recovery checks need no repeat.

Candidate7's forward Climbing Ridge Cut and rebuilt Summit Traverse both pass with zero missed gates or recoveries. Reverse Summit Traverse then exposed a second, separate tracking condition: `RaceDirector` treated a nearby later main-road station as being past the exit even while the racer remained on the branch centerline. The mountain-only condition now requires actual lateral departure before that abandonment. Candidate8's reverse Summit Traverse passes (416.7/421.4 m before the legitimate exit, zero misses/recoveries). All four alternates and both local recoveries are complete.

Candidate7's main attempt stopped at a collision/recovery on the climb, with obstacle diagnostics identifying `EMBER` at 0.2 m. Mountain navigation still used the inherited Forest half-width of 3.6 m despite a supported six-metre half-width. Candidate8 uses that actual supported width, separate initial lane choices and clear adjacent passing lanes for mountain racers. Player motor/handling and other-course lane behavior are retained. Final main-line laps remain pending.

Candidate8's next main attempt passed the gully but pitched over on the climb without a racer collision. The contact log identifies coarse `Ground_Mountain_2_2` terrain immediately below the smooth trail. `climb-surface-audit.txt` measures the existing smooth ribbon across all four driving lanes; the terrain contact has a sharper normal than that ribbon. The focused correction lowers only the underlying coarse terrain in the affected forward segment, retaining its supported road mesh, route and vehicle forces. A local climb check will verify that correction before another full-lap attempt.

The first local check (`final9/climb`) cleared the original bump but found another coarse-terrain contact higher on the same climb, outside the initial correction bounds. The correction was extended through that climb to the flat summit approach. The complete bounded segment now passes in `final10/climb`: minimum up 0.908, no recovery, ordinary motor inputs from rest. Only then was the main playlist attempt resumed.

The forward motorcycle full lap now passes in `final10/playlist`: 109.934 seconds, zero missed gates/recoveries, gully flight 44.80 m/s / 6.24 s / 215.12 m, summit flight 40.13 m/s / 6.82 s / 195.51 m, and the supported return completed. Results reopening did not double-award; restart cleared the provisional result. Two rivals physically finished (111.744 and 112.927 seconds); the ATV rival reached the unchanged 90-second grace limit and DNF, so the all-three-AI assertion failed.

The ensuing player ATV attempt diagnosed that DNF's cause: a zero-distance overlap from the ATV's wider obstacle sensor identified its own `Ground_CR117 gully takeoff` as a wall and applied full brakes. Takeoff fell to 19.71 m/s and the attempted lap failed. The corrected sensor ignores such an overlap only within a committed mountain flight and only after a downward ray against that same collider confirms a walkable surface beneath the vehicle. Cars, trees and actual walls remain obstacles.

The completed motorcycle lap is retained rather than repeated. Candidate11 resumes the remaining three played events using a provisional replacement fixture reconstructed from those retained standings. `final10/playlist/provisional-resume-notes.txt` identifies its displayed time precision and unset unfinished DNF time. Restart clears the fixture before driving; no fixture points or times contribute to final championship totals. Forward ATV and reverse ATV use measured AI finishes, with reverse motorcycle retaining the existing estimated-finish policy.

Candidate11's ATV gully run passes (42.78 m/s, 5.98 s, 197.13 m), proving the zero-distance sensor fix. Its summit run still failed: the sensor braked for `Ground_Mountain_1_4` under the launch at 13.4/8.2/3.0 m, reducing takeoff to 33.26 m/s. The contact log then records a collision with the front of the catch terrain. The simple airtime/distance assertion printed PASS, but the physical collision and failed lap supersede it; this is not an accepted flight. The subgrade correction now extends beneath the summit approach/runway. A local ATV diagnostic was prepared but not run: Dan stopped further track testing. The final subgrade correction is compiled and unverified in motion; no full-lap testing resumes under this preview scope.

## Reduced delivery gate (completion recorded below)

- Small synthetic three-event scoring/UI fixture; no further races or AI events.
- Two final-player spoken-title captures, one reused as extracted portable startup.
- Complete runtime/ZIP/Latest, cover/music/settings preservation and package hashes.

The retained evidence includes one complete forward motorcycle lap, eight initial centered flight cases, all four local alternates and both local recoveries. The attempted forward ATV laps and AI finish assertions include failures documented above. The last summit subgrade correction compiled but was not run after Dan stopped track testing. No reverse main-line lap, complete all-vehicle matrix, final AI reliability, or human fun approval is claimed. Former "next candidate" and "pending lap" passages above are historical investigation notes, superseded by this reduced gate.

## Spoken title and listening limits

`title-source-comparison.json` proves the supplied MP3 decodes identically to the imported `Voice.wav`, 1.750208 seconds; its final half-second contains signal. The confirmed early-input defect stopped the voice coroutine/source immediately. The voice now survives the menu transition, and radio remains silent until the complete scheduled sample tail ends. Original recording, artwork and theme are retained. Both final-player captures pass the full-phrase signal comparison. Packaged PCM is identical to the imported reference (84,010 stereo frames at 48 kHz); full correlation is 0.99999977 and final 350-ms correlation is 0.99997505 in both launches. No clipping was recorded. Untouched startup retains the looping theme after speech; early advance reaches the menu while the original voice survives and completes. Output is remuted immediately afterward. See title-final-output-comparison.json.

The available audio-input tool explicitly cannot audition audio. Listener-DSP captures are before the OS endpoint; they do not establish subjective listening, speaker/headphone behavior or physical Steam Deck/controller acceptance. Fun and audible human acceptance remain Dan's decisions.

## Regeneration and review

`Racer > Author CR112-118 from reviewed baseline` applies the current authoring recipe to baseline scenes. Passes reject already-authored geometry to avoid duplication. See `README-player.txt` for the seven-item review checklist, viewing locations, playlist scoring rules and controls.

## Final preview scoring fixture
Final player synthetic three-event checks pass: YOU 20, Rival 1 26, Rival 2 12, Rival 3 6; winner Rival 1. Restart clears the provisional entry and winner state; replacement/reopening does not duplicate points; unfinished quit clears the championship. All three event pages captured and visually inspected. These are injected data fixtures, not three completed races. The Next transition guard was inspected; no additional race matrix was run.



## Final package and startup outcome
Build 0.20.0-review1 succeeded with zero errors and three warnings. The complete Windows runtime, ZIP and Latest contain 187 playable staged songs plus two preserved originals, licenses, the standalone accepted cover and both route maps. All 451 packaged files were SHA256 verified across the ZIP extraction, runtime and Latest. Previous Latest/music and prior runtime were preserved; real settings and saves were not used by checks.

Exactly two final-player audio launches were used. The untouched launch ran from the extracted ZIP and successfully loaded the title, complete voice and continuing theme. Its immediate post-dismissal menu assertion failed: the fixture sampled after a fixed four frames, while EnterMenu waits for release and additional frames. That screenshot is the transition with artwork hidden and menu not yet drawn; it is not a verified portable-menu pass. The other launch demonstrably reached the menu during speech, completed the identical ending, and was remuted. No third audio launch or rebuild was performed to repair this diagnostic timing assertion. Portable startup is demonstrated through the title; the recorded assertion failure is retained. A human/OS-endpoint listen was unavailable, so audible acceptance remains open.

The compiler warnings and Unity-generated empty-name whitespace are recorded without expanding this task into unrelated cleanup. The serialized camp material lost a redundant emission keyword during the scene authoring/import pass; camp presence remained visible in the targeted household captures.


var live=UnityEngine.Object.FindAnyObjectByType<Racer.RaceDirector>();
var go=new UnityEngine.GameObject("Isolated rules replay");go.SetActive(false);var test=go.AddComponent<Racer.RaceDirector>();test.vehicle=live.vehicle;test.gates=live.gates;test.road=live.road;test.laps=1;go.SetActive(true);test.enabled=false;
var road=test.road;float origin=road.Project(test.gates[0].transform.position,out _);var log=new System.Collections.Generic.List<string>();double clock=0;
void Point(UnityEngine.Vector3 p,UnityEngine.Vector3 f){clock+=.05;test.Sample(p,f,clock);}
void Begin(){test.Progress.Restart();test.Racers[0].FinishArmed=false;test.Racers[0].Travel=0;clock=0;var p=road.At(origin-3,out var f)+UnityEngine.Vector3.up*.7f;test.ResetSampling(p,0);}
void Road(float from,float to,int miss1=-1,int miss2=-1){for(float s=from;s<=to;s+=2){var p=road.At(origin+s,out var f);foreach(int gate in new[]{miss1,miss2})if(gate>0){float gs=road.Relative(road.Project(test.gates[gate].transform.position,out _),origin);if(UnityEngine.Mathf.Abs(s-gs)<30)p+=UnityEngine.Vector3.Cross(UnityEngine.Vector3.up,f).normalized*9;}Point(p+UnityEngine.Vector3.up*.7f,f);}}
Begin();Road(-1,road.Length+3);log.Add($"Normal: finished={test.Progress.Finished}, misses={test.Progress.MissedGates}, penalties={test.Progress.PenaltySeconds}");
Begin();Road(-1,road.Length+3,1);log.Add($"Single bypass: finished={test.Progress.Finished}, misses={test.Progress.MissedGates}, penalties={test.Progress.PenaltySeconds}");
Begin();Road(-1,road.Length+3,1,2);log.Add($"Two bypasses: finished={test.Progress.Finished}, misses={test.Progress.MissedGates}, penalties={test.Progress.PenaltySeconds}");
Begin();Road(-1,20);for(int i=0;i<5;i++){Road(20,-1);Road(-1,20);}log.Add($"Finish abuse: laps={test.Progress.CompletedLaps}, penalties={test.Progress.PenaltySeconds}");
Begin();Road(-1,25);var a=road.At(origin+25,out _);var b=road.At(origin+road.Length*.55f,out _);var c=road.At(origin+road.Length-12,out _);
void Cut(UnityEngine.Vector3 from,UnityEngine.Vector3 to){int count=UnityEngine.Mathf.CeilToInt(UnityEngine.Vector3.Distance(from,to)/2);var f=(to-from).normalized;for(int i=1;i<=count;i++)Point(UnityEngine.Vector3.Lerp(from,to,i/(float)count)+UnityEngine.Vector3.up*.7f,f);}
Cut(a,b);Cut(b,c);Road(road.Length-10,road.Length+3);log.Add($"Large cut: finished={test.Progress.Finished}, misses={test.Progress.MissedGates}, driving={test.Progress.RaceTime(clock):0.00}, penalties={test.Progress.PenaltySeconds:0.00}, adjusted={test.Progress.AdjustedTime(clock):0.00}");
Begin();var shortcut=Racer.Editor.Phase5Setup.Path();float entry=road.Relative(road.Project(shortcut[0],out _),origin),exit=road.Relative(road.Project(shortcut[shortcut.Count-1],out _),origin);Road(-1,entry);for(int i=1;i<shortcut.Count;i++)Point(shortcut[i]+UnityEngine.Vector3.up*.7f,(shortcut[i]-shortcut[i-1]).normalized);Road(exit,road.Length+3);log.Add($"Approved shortcut: finished={test.Progress.Finished}, misses={test.Progress.MissedGates}, penalties={test.Progress.PenaltySeconds}");
Begin();float jump=road.Relative(road.Project(new UnityEngine.Vector3(-627,0,-110),out _),origin);for(float s=-1;s<=road.Length+3;s+=2){var p=road.At(origin+s,out var f);float z=s-jump;if(z>=0&&z<85)p.y+=UnityEngine.Mathf.Sin(z/85*UnityEngine.Mathf.PI)*7;Point(p+UnityEngine.Vector3.up*.7f,f);}log.Add($"Jump route replay: finished={test.Progress.Finished}, misses={test.Progress.MissedGates}, penalties={test.Progress.PenaltySeconds}");
Begin();Road(-1,10);for(int n=0;n<5;n++){for(float s=10;s>=-3;s-=1){var p=road.At(origin+s,out var f);Point(p+UnityEngine.Vector3.up*.7f,-f);}Road(-1,10);}log.Add($"Continuous wrong-way/repeated finish: laps={test.Progress.CompletedLaps}, misses={test.Progress.MissedGates}");
Begin();Road(-1,10);var tele=road.At(origin+road.Length*.7f,out var tf);Point(tele+UnityEngine.Vector3.up*.7f,tf);Road(road.Length*.7f+2,road.Length*.7f+30);log.Add($"Teleport then forward: misses={test.Progress.MissedGates}, lapActive={test.Progress.LapActive}");
UnityEngine.Object.Destroy(go);System.IO.File.WriteAllLines("Docs/CR022-025/route-rules.txt",log);return string.Join("\n",log);


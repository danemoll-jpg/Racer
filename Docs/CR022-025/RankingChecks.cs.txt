var live=UnityEngine.Object.FindAnyObjectByType<Racer.RaceDirector>();var go=new UnityEngine.GameObject("Isolated classification check");go.SetActive(false);var r=go.AddComponent<Racer.RaceDirector>();r.vehicle=live.vehicle;r.gates=live.gates;r.road=live.road;r.laps=1;go.SetActive(true);r.enabled=false;
r.Racers.Clear();var first=new Racer.RacerState("physical first",live.vehicle,2,1);var second=new Racer.RacerState("adjusted winner",live.vehicle,2,1);r.Racers.Add(first);r.Racers.Add(second);
first.Progress.BeginTiming(0);first.Progress.Cross(0,true,1);first.Progress.Miss(1,20);first.Progress.Cross(2,true,90);first.Progress.Cross(0,true,100);
second.Progress.BeginTiming(0);second.Progress.Cross(0,true,1);second.Progress.Cross(1,true,50);second.Progress.Cross(2,true,90);second.Progress.Cross(0,true,105);
bool order=r.Ordered(true)[0]==second;var third=new Racer.RacerState("stuck",live.vehicle,2,1);r.Racers.Add(third);r.maximumRaceSeconds=0;typeof(Racer.RaceDirector).GetMethod("FixedUpdate",System.Reflection.BindingFlags.Instance|System.Reflection.BindingFlags.NonPublic).Invoke(r,null);bool bounded=third.Dnf&&r.ClassificationFinal;
string result=$"Adjusted classification beats physical finish order: {order}\nBounded DNF/final classification: {bounded}\n"+r.Standings();System.IO.File.WriteAllText("Docs/CR022-025/classification.txt",result);UnityEngine.Object.Destroy(go);return result;
